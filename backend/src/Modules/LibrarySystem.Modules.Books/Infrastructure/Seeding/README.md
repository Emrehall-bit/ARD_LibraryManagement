# Books Development Seed Data

`books.seed.json` contains synthetic catalogue entries for development testing of listing, search, stock and borrowing scenarios. It is not a production catalogue.

Regenerate it with `generate-books-seed.ps1`; the generator is deterministic and produces 5000 records with `name`, `author` and `stock`.
